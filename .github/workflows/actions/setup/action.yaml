name: 'Setup Symfony'
description: 'Setup Symfony project for testing and analysis'
runs:
  using: 'composite'
  steps:
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.4'

    - name: Install Composer
      uses: ramsey/composer-install@v2
